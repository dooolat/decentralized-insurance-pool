name: Bootstrap CI

on:
  push:
  pull_request:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Bootstrap repository only. CI will be expanded in the next milestone."
